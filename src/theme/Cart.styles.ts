import {StyleSheet} from 'react-native';

export const cartStyles = StyleSheet.create({
  container: {
    flex: 1,
  },

  removeButton: {
    backgroundColor: 'red',
    borderColor: 'red',
  },
});
